# V0.5.2 — Actual Change / Diff Capture + Workspace Integrity

Baseline: `f1aac14c0cefaaf0866003773c8132e48b8a78e0` (`0.5.1A`).

## API and evidence contract

Use `manager.captureWorkspaceChanges(taskId, options?)`. The manager owns the runner,
trusted workspace inspection and the same repository/task operation fence as create/remove.
Equivalent option snapshots join the exact promise, incompatible concurrent operations reject.

Evidence separates durable base→HEAD, HEAD→index, index→worktree, untracked fingerprints,
conflict stages and ignored diagnostics. `workingFiles` contains raw SHA-256 fingerprints;
clean changed gitlinks carry their nested HEAD OID because Git's unstaged raw OID is zero.
Dirty submodules and incomplete index flags reject capture. No recursive submodule inventory.

Two complete passes compare identity, status, index stages/flags, all source metadata,
working bytes, ignored reports and optional patches. Every returned object/array is frozen.
The source digest excludes absolute paths, ignored outputs and patch presentation options.
Canonical ordering uses explicit JavaScript string comparison and sorted object keys.
Two observed equal passes are not an operating-system transaction or a lock on external writers.

Defaults: 20,000 changed paths, 1 GiB filesystem bytes per pass, 512 KiB total returned patch
text, 1,000 ignored path samples. Ignored count means Git-reported entries, possibly directories.
Runner structured-output overflow fails closed. Patch-only overflow preserves the inventory
and returns `omitted-limit`. Patches stay in returned memory; there is no event or DB integration.
Unrepresentable UTF-8 paths, unsafe Windows paths, special files and nested untracked repositories
fail safely. Symlink hashes cover link target bytes, never target file content.

Capture uses `--no-optional-locks`, disables fsmonitor, external diff, textconv, color and rename
heuristics, and disables object replacement and lazy fetching. Git supporting `--no-lazy-fetch`
is required (documented in [Git 2.45](https://git-scm.com/docs/git/2.45.0)); the exact guarded
`--version` invocation preflights capability before source collection. There is no fallback that
permits network access. Verified runtime: Git 2.51.1, Node 24.19.0, Linux. Windows validation is
performed by the repository's existing Windows/Node 22 GitHub Actions workflow after pushing.

## Scope

Production changes: `src/workspace/GitWorkspaceChangeCapture.ts`, narrow manager method and
workspace barrel exports only. Git runner, V0.4 runtime/providers, database, dependencies and
workflows are unchanged. Capture never executes project builds/tests or review/merge operations.
Developer tests do create disposable repositories and commits; those are not product behavior.
New real model calls: 0.

## Pre-commit failure matrix / reviewer search

The following 30 requested candidates were investigated through regression tests and final source review.
"Contract" means the relevant emitted arguments / source boundary are asserted or inspected;
"real" means disposable native Git repositories, with deterministic callbacks for mutations.

| # | Candidate | Evidence / result |
|---|---|---|
| 1 | Committed changes omitted | Real durable-base committed layer |
| 2 | Staged/unstaged collapse | Real same-file mixed layers and union |
| 3 | Untracked omitted | Real path, size, SHA and union |
| 4 | Ignored digest contamination | Real ignored creation; digest unchanged |
| 5 | Same-status tracked edit | Real two-pass mutation rejected |
| 6 | Same-path untracked edit | Real two-pass mutation rejected |
| 7 | Task HEAD moves | Real between-pass ref movement rejected |
| 8 | Durable marker moves | Real between-pass marker movement rejected |
| 9 | Capture/remove race | Same-manager/cross-manager busy fence |
| 10 | Capture/create race | Both operation directions fenced |
| 11 | Second manager bypass | Identical request shares promise |
| 12 | Other tasks serialize | Different-task concurrent capture completes |
| 13 | Rename config | Real rename represented as A/D with config enabled |
| 14 | External diff executes | Explicit `--no-ext-diff` assertions |
| 15 | Textconv executes | Explicit `--no-textconv` assertions |
| 16 | Binary counted as zero | Parser + real binary: null line counts |
| 17 | Tabs/newlines | Parser fixtures + Linux real paths |
| 18 | Conflict appears clean | Real merge conflict + retained stage OIDs |
| 19 | Dirty submodule omitted | Parser + real nested dirty file rejection |
| 20 | Assume-unchanged | Parser + real rejection and retry |
| 21 | Skip-worktree | Parser + real rejection and retry |
| 22 | Large patch fails inventory | Configured bound + injected runner overflow |
| 23 | File count truncated | Hard limit failure and successful retry |
| 24 | Whole large file loaded | Fixed 64 KiB buffer; multi-buffer real hash |
| 25 | Symlink target followed | Real external-target edit leaves link digest stable |
| 26 | FIFO hangs | Deterministic regular→FIFO replacement rejected |
| 27 | Patch leaks in errors | Injected secret exception becomes bounded code |
| 28 | Patch changes source digest | Real enabled/omitted/disabled equality |
| 29 | Ignored build output changes digest | Real ignored file and directory |
| 30 | Failed capture remains busy | Real failures followed by successful retry |

Additional 12 candidates derived from the implementation diff:

| # | Candidate | Evidence / result |
|---|---|---|
| 31 | Entire deleted parent directory | Reproduced and fixed; real deletion fingerprint |
| 32 | Conflict U/M row-stat misalignment | Reproduced and fixed; explicit separate line assertions |
| 33 | Consecutive gitlink moves use zero OID | Reproduced and fixed; two clean HEAD advances change digest |
| 34 | Status silently refreshes index | Real index byte comparison; optional locks disabled |
| 35 | fsmonitor launches a helper | Command assertions disable core.fsmonitor |
| 36 | Parent replaced with junction/link | Deterministic substitution rejected before hashing |
| 37 | Nested repository silently skipped | Real explicit rejection |
| 38 | Ignored changes beyond sample missed | Fake full-pass comparison with zero returned sample |
| 39 | Index staging changes mid-pass | Real deterministic staging mutation rejected |
| 40 | Late failed-generation consumers clear B | Real duplicate failure + active B + remove rejection |
| 41 | Distinct repositories share fence | Held A while B completes |
| 42 | Intent-to-add + large working bytes | Real 200,000-byte fingerprint and unstaged layer |

Additional reviewed boundaries: Git version capability failure, unknown record kinds, malformed
raw/stat/index records, unsafe paths, object freezing, options getter snapshots, stat identity
checks before/after reads, raw/stat join completeness, patch budget across layers, and sanitized
filesystem failures. Pairing metadata uses path buckets rather than a quadratic scan.

Problem-cluster issues fixed proactively: 3 (rows 31–33).
Likely-next-reviewer issues actively investigated: 42.
Additional failure paths discovered proactively: 3.
Additional regressions added proactively: 19 (12 complete-pass unit cases + 7 real Git cases
added after the initial 65-case implementation matrix; existing conflict case also strengthened).
Same-root-cause issues intentionally deferred: 0.

## Verification record

The first literal `npm test` was run before any environment exclusions:
845 passed, 7 skipped, 5 failed. One new gitlink regression was subsequently fixed. Four historical
failures were the 5-second 25-process restart test, Windows-only `.cmd` execution on Linux, absent
installed Codex CLI detection and absent real Codex handshake. These are recorded, not reported green.

Reduced-worker attempt:
`CI=true npx vitest run --maxWorkers=2 -t '^(?!.*runs a path-with-spaces cmd shim)'`
reported 846 passed, 10 skipped, 1 failed. The only failure was the historical 5-second process
restart test. `CI=true` uses the existing two real-Codex exclusions; the name filter excludes only
the Windows `.cmd` case. No test source or production timeout was changed to mask this result.
This attempt preceded the final multi-buffer/intent-to-add test addition.

The restart case also exceeded its original 5-second deadline in isolation. A separately labelled
15-second, single-test diagnostic can distinguish host spawn latency from functional failure;
this is not an assertion that the original deadline passed. The untouched Windows CI remains the
full-suite gate for this Windows-targeted repository.

Final scoped capture suite and static gates: see release verification below.
Historical Git baseline consists of 61 cases (9 runner, 37 worktree unit/failure, 15 integration).
New capture suite consists of 54 unit/failure cases and 30 native Git integration cases on Linux.
Windows legitimately omits two POSIX-only filenames, one file-symlink case and one FIFO case;
parser fixtures still exercise unusual names on all platforms and junction containment runs on Windows.

## Local verification still required

On the user's Windows 11 machine with the existing Codex installation, run these without `CI=true`:

```powershell
npx vitest run tests/codex-protocol.test.ts tests/codex-process.integration.test.ts --maxWorkers=2
```

These validate the installed CLI/version assumption and real app-server initialization/shutdown.
They do not require sending a model prompt. The seven explicitly opt-in real Claude tests require
the user's authenticated CLI environment and may invoke real models; they were not enabled for
this zero-model-call stage. Inspect their existing environment gates before running them.

Also run `npm test` on Windows locally if local process timing is part of acceptance. GitHub Actions
covers native Windows `.cmd` behavior, but cannot validate the user's own CLI login, PATH, antivirus,
filesystem permissions or local installation. No V0.5.3 work has begun.

## Pre-push release verification

- Typecheck: PASS.
- Build: PASS.
- Lint: PASS.
- `git diff --check`: PASS.
- V0.5.1A historical Git tests: 61/61 PASS in the full suite.
- V0.5.2 final unit tests: 54/54 PASS.
- V0.5.2 final native Git integration: 30/30 PASS.
- Default-concurrency local full suite: NOT PASS; exact first-run failures documented above.
- Reduced-worker local suite: NOT PASS; one historical host-timing failure documented above.
- Isolated restart diagnostic with 15-second test-runner deadline: PASS, 6.516 seconds;
  original 5-second test still not passed on this host.
- New model calls: 0; no product build/test/review/merge behavior added.
- GitHub main/tag CI: checked after push; authoritative run links belong to the release handoff.

Pre-commit Failure Matrix:
PASS (V0.5.2 source-capture cases; local full-suite environment failures remain explicitly separate)

Additional failure paths discovered proactively:
3

Additional regressions added proactively:
19

Unresolved failure paths:
NONE in the implemented capture cluster. Native user CLI checks remain unverified.

## Actual publication result

Publication is BLOCKED, not completed. Command-line `git push origin main` failed because this
execution environment has no GitHub username/credential. The connected GitHub application's
create-tree endpoint then returned HTTP 403 `Resource not accessible by integration`.
Repository metadata's `permissions.push=true` describes the user's repository permission; it did
not grant the connected integration effective contents-write access. No endpoint workaround was
attempted after that authorization denial.

Remote main remains at the audited baseline; no V0.5.2 tag was created, and new main/tag CI is
NOT VERIFIED. Restore repository contents-write authorization to complete publication. The local
commit, git bundle and patch preserve the completed implementation for import without rewriting
existing history. No claim of full release completion or CI success is made.

Process cleanup: 0 test-owned Codex/Claude/Git subprocesses and 0 capture temporary repositories
remained after verification. Host Codex infrastructure processes were left untouched.
